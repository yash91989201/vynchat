name: vynchat

services:
  postgres:
    image: postgres
    container_name: vynchat-postgres
    environment:
      POSTGRES_DB: vynchat
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - vynchat_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  vynchat_postgres_data: